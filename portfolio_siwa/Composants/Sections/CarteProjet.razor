@namespace portfolio_siwa.Composants.Sections

@using portfolio_siwa.Donnees

<article class="carte @(this.Inverse ? "carte--inverse" : "")" data-reveal>
    <div class="carte__visuel">
        @* Le visuel occupe environ 564 px au-dessus de 860 px, toute la largeur en dessous. *@
        <img src="@this.Fiche.Image" srcset="@this.Fiche.SrcSet" sizes="(min-width: 861px) 564px, 100vw"
             alt="@($"{Traductions.ApercuDe[this.Langue]} {this.Fiche.Titre[this.Langue]}")"
             width="@this.Fiche.ImageLargeur" height="@this.Fiche.ImageHauteur"
             loading="lazy" decoding="async" />
    </div>

    <div class="carte__corps">
        <p class="etiquette">@this.Fiche.Categorie[this.Langue]</p>
        <h3 class="carte__titre">@this.Fiche.Titre[this.Langue]</h3>

        @if (this.Contexte.Count > 0)
        {
            <p class="carte__contexte">@string.Join(" · ", this.Contexte)</p>
        }

        <div class="carte__resume mesure">@this.Fiche.Resume.Balise(this.Langue)</div>

        @* Le rôle apparaît au survol, et au toucher grâce au focus : rien n'allonge la page. *@
        <ul class="puces">
            @foreach (var techno in this.Fiche.Technos)
            {
                <li>
                    <button type="button" class="puce" aria-expanded="false">
                        <img src="@techno.Logo" alt="" loading="lazy" />
                        <span class="puce__nom">@techno.Nom</span>
                        <span class="puce__role">@techno.Role[this.Langue]</span>
                    </button>
                </li>
            }
        </ul>

        @if (this.Fiche.LienSite is not null || this.Fiche.LienGithub is not null)
        {
            <div class="carte__liens">
                @if (this.Fiche.LienSite is not null)
                {
                    <a class="bouton bouton--plein" href="@this.Fiche.LienSite"
                       target="_blank" rel="noopener noreferrer">@Traductions.VoirLeSite[this.Langue]</a>
                }
                @if (this.Fiche.LienGithub is not null)
                {
                    <a class="bouton bouton--contour" href="@this.Fiche.LienGithub"
                       target="_blank" rel="noopener noreferrer">@Traductions.CodeSource[this.Langue]</a>
                }
            </div>
        }
    </div>
</article>
